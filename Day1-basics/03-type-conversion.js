// Farenheit to celcius

let farenheit = 78;
let celcius = (farenheit-32)*(5/9);

console.log(`${farenheit}°F = ${celcius.toFixed(1)}°C`);

//toFixed() => coverts the number to a string data type
//Number() => can convert string back to number